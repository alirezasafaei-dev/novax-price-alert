{
  "name": "novax-telegram-relay-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node test/log.test.mjs && node test/telegram-relay.test.mjs && node test/new-bot.test.mjs",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
